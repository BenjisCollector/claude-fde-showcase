"""claude-fde-showcase: a runnable demonstration of the Forward Deployed
Engineer / Applied AI skillset (MCP servers, sub-agents, agent skills).

The core tool logic lives in :mod:`claude_fde_showcase.tools` and depends only on
the standard library, so it imports and runs without the ``mcp`` SDK installed.
"""

__version__ = "0.2.0"
